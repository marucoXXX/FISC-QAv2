[project]
name = "fisc-qav2"
version = "0.1.0"
description = "FISC アンケート自動回答マルチエージェントシステム"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.20",
    "openpyxl>=3.1",
    "python-docx>=0.8",
]

[project.optional-dependencies]
web = [
    "fastapi>=0.100",
    "uvicorn[standard]>=0.20",
    "python-multipart>=0.0.6",
]
dev = [
    "pytest>=7.0",
    "reportlab>=4.0",
    "httpx>=0.24",
]
pdf = [
    "PyMuPDF>=1.23",
]

[project.scripts]
fisc-qav2 = "src.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "e2e: end-to-end pipeline tests",
    "slow: tests that take a long time to run",
]

[tool.ruff]
target-version = "py39"
line-length = 100
