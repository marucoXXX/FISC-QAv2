# FISC-QAv2 ロードマップ

## 完了済みマイルストーン

### P1: 全エージェント実装 + テストフィクスチャ生成
- 全5エージェント（Orchestrator / Indexer / Router / Reader / Reviewer）実装
- テストフィクスチャ自動生成（KB 12ファイル + 質問票20問）
- 27テストパス

### P2: テスト強化
- Reader / Reviewer の mock テスト追加
- Excel 出力検証テスト追加
- 38テスト全パス（API不要）

---

## 今後の開発計画

### P3: Indexer 先頭ページ要約改善

**目的**:
インデックスの要約品質を向上させ、後続の P4 Router 動的化で LLM がルーティング判断に使う「ドキュメント概要」の精度を引き上げる。

**現状の課題**:
現在の `_extract_summary_*()` は、各KBファイルの **先頭500文字** を機械的に切り出しているだけである。
これには以下の問題がある:

1. **PDF**: PyMuPDF がインストールされていない場合、テキスト抽出自体ができずファイルサイズからのトークン推定のみになる。要約は空文字になりうる
2. **DOCX / XLSX**: 先頭500文字はタイトルや目次部分に当たることが多く、文書の実質的な内容（例えばセキュリティポリシーの適用範囲、バックアップのRPO/RTO定義など）が含まれない
3. **トークン推定**: 日本語2文字=1トークン / 英語4文字=1トークンという粗い比率で推定しており、実際の Claude トークナイザとの乖離がある。この誤差が Router のビンパッキング精度に影響する

**実装方針**:

| 変更内容 | 詳細 |
|----------|------|
| PDF先頭ページ限定 | `_extract_summary_pdf()` を先頭1〜2ページに限定。PyMuPDF未インストール時は `reportlab` フォールバックも検討 |
| DOCX構造的抽出 | 見出し（Heading）段落を優先抽出し、本文は先頭N段落に制限。目次ページを除外するヒューリスティック追加 |
| LLM要約オプション | `Config` に `use_llm_summary: bool = False` を追加。有効時は先頭ページテキストを Claude API に送り、100〜200トークン程度の構造化サマリを生成 |
| トークン推定改善 | `anthropic.count_tokens()` が使えれば正確値を採用、なければ従来の文字数比率をフォールバック |

**業務への影響**:
- FISC質問票の質問は「セキュリティ管理」「アクセス管理」「バックアップ管理」等の大分類に分かれており、現状の Router はこの大分類テキストとファイル名のキーワード一致で割り当てを行っている。要約が改善されると、P4 で LLM Router に切り替えた際に「このドキュメントにはMFA設定手順が含まれる」といった内容ベースのルーティングが可能になり、**関連度の低いKBファイルをReaderに渡す無駄が減る**
- 結果として、回答の根拠が的確になり、Reviewer での「根拠ソース不一致」指摘が減少する

**FISC制度面の影響**:
- FISC安全対策基準では、回答の根拠として適切な社内文書を引用することが求められる。要約品質が上がることで、正しいドキュメントが正しい質問に割り当てられる確率が向上し、**監査時の「回答根拠の妥当性」に対する信頼性が向上** する
- 現状の500文字切り出しでは、文書の網羅範囲を判断できない場合がある（例: DR/BCP計画書の先頭500文字が「目次」のみの場合、バックアップ関連の質問に割り当てられない可能性がある）

**トークン使用への影響**:

| シナリオ | 追加トークン消費 | 備考 |
|----------|-----------------|------|
| LLM要約OFF（デフォルト） | **0** | ローカル処理のみ。API呼び出しなし |
| LLM要約ON | KBファイル数 × 約1,000〜2,000トークン（入力） + 約200トークン（出力） | KB 12ファイルの場合、合計約15,000〜26,000トークン |
| パイプライン全体への間接効果 | Reader へのKB入力トークンが **削減** される可能性あり | 精度向上により不要なファイル割当が減るため |

現在のパイプラインでは Indexer は API を一切使っていない（ゼロトークン）。LLM要約をオプトインした場合でも、インデックス生成は通常1回だけ実行し、キャッシュ（`.fisc_index_cache.json`）で再利用するため、ランニングコストへの影響は軽微。

**影響範囲**:
- `src/indexer.py` 内部のみ。`run_indexer()` の引数・戻り値（`list[IndexEntry]`）は不変
- `src/config.py` に `use_llm_summary` フィールド追加
- テストフィクスチャ `tests/fixtures/expected/index.json` の summary フィールド更新が必要

---

### P4: Router 動的ルーティング化

**目的**:
静的キーワードマッチ（`_KEYWORD_MAP` による9カテゴリ × 固定ファイル名マッピング）を LLM ベースの動的割当に置き換え、**質問文の意味を理解した高精度なルーティング** を実現する。

**現状の課題**:
現在の Router は `_KEYWORD_MAP` という辞書で、質問の大分類テキスト（例: "セキュリティ管理"）をKBファイル名のステム（例: "security_policy"）にマッピングしている。

この方式の限界:
1. **大分類に依存**: 質問文の内容（小分類や具体的な質問テキスト）を一切見ていない。「セキュリティ管理」配下でも、暗号化の質問と入退室管理の質問では参照すべきKBが異なる
2. **KBファイル追加時にコード変更が必要**: 新しいKBファイルを追加した場合、`_KEYWORD_MAP` を手動更新しないとルーティング対象にならない
3. **フォールバックの粒度が粗い**: マッチしない場合 `_CATEGORY_HINTS` → "policies" or "system_docs" カテゴリの先頭2ファイルという雑なフォールバックになる
4. **過去回答再利用判断にも影響**: Orchestrator の `_find_related_files()` も同じ `_KEYWORD_MAP` を参照しており、ルーティング精度が過去回答の再利用判断に波及する

**実装方針**:

| 変更内容 | 詳細 |
|----------|------|
| `run_router()` シグネチャ拡張 | `api_key`, `model` パラメータ追加。未指定時は従来の静的ルーティングにフォールバック |
| LLMルーティングプロンプト | 全質問 + 全KBインデックス（ファイル名・カテゴリ・要約・推定トークン数）を1回のAPI呼び出しで送信。質問ごとに関連ファイルリストを JSON で返却させる |
| Orchestrator 対応 | `_find_related_files()` を Router の出力（`RoutingResult`）から直接参照するように変更。過去回答再利用判断の精度も連動して向上 |
| フォールバック | API失敗時・タイムアウト時は既存の `_KEYWORD_MAP` ベースに自動切替。ログに警告出力 |
| ビンパッキング維持 | `_pack_readers()` のロジック（80,000トークン上限のグリーディパッキング）はそのまま継続利用 |

**業務への影響**:
- **回答精度の直接的な改善**: 現状、20問の質問票に対して Reader が参照するKBファイルの的中率は、大分類ベースのマッチングに制約されている。動的ルーティングにより、例えば「多要素認証の導入状況」という質問に対して `access_control.docx` だけでなく `security_policy.pdf` の該当セクションも割り当てるといった柔軟な判断が可能になる
- **KB拡張時の運用コスト削減**: 新規KBファイル追加時に `_KEYWORD_MAP` の手動メンテナンスが不要になる。KBディレクトリにファイルを置くだけで自動的にルーティング対象になる
- **過去回答再利用の精度向上**: `_find_related_files()` がルーティング結果と連動するため、「本当に関連するKBファイルが更新されたかどうか」の判定が正確になり、不要な再回答を減らせる

**FISC制度面の影響**:
- FISC安全対策基準の質問項目は年度ごとに改訂される。新しい質問カテゴリが追加された場合、静的マッピングでは対応漏れが発生しうるが、LLMベースであれば **質問文の意味から適切なKBを推定できる** ため、制度改訂への追従コストが大幅に下がる
- 複数の基準項目にまたがる横断的な質問（例:「クラウド環境におけるデータ保護とバックアップの両方の対策」）に対して、複数カテゴリのKBを同時に割り当てられるようになる。現状の静的マッピングでは大分類1つにしかマッチしない

**トークン使用への影響**:

| 項目 | トークン消費 | 備考 |
|------|-------------|------|
| Router LLM呼び出し（新規） | 入力: 約3,000〜8,000トークン（質問20問 + KB要約12件）、出力: 約500〜1,000トークン | パイプラインあたり **1回** のみ |
| Reader への間接効果 | KBファイル入力が **精選** されることで、Reader 1台あたりの入力トークンが削減される可能性 | 不要なファイルを渡さなくなるため |
| 過去回答再利用率の向上 | Reader/Reviewer の API呼び出し自体が **減少** する可能性 | 再利用判定の精度向上により、新規回答が必要な質問数が減る |
| 合計影響 | Router 単体で +4,000〜9,000トークン/回。ただしReader削減効果で **相殺〜純減** の可能性あり | |

現在のパイプライン全体のトークン消費は、Reader（80,000トークン上限 × Reader台数）+ Reviewer（全回答レビュー1回）で構成されている。Router に1回の API 呼び出しを追加するコストは、パイプライン全体の1〜5%程度に収まる見込み。

**影響範囲**:
- `src/router.py`（主要変更）、`src/orchestrator.py`（`_find_related_files()` 修正）
- `src/config.py`（`use_llm_router: bool = True` 追加検討）
- テストは mock 化で API 不要を維持

**前提**: P3 完了（高品質なインデックス要約がルーティング精度に直結）

---

### P5: CI/CD パイプライン

**目的**:
GitHub Actions で pytest を自動実行し、PR マージ前の品質ゲートを設ける。また、`pyproject.toml` に依存関係を明記して、新規開発者のセットアップコストを下げる。

**現状の課題**:
1. **依存関係が暗黙的**: `anthropic`, `openpyxl`, `python-docx` 等の依存が `pyproject.toml` に明記されておらず、手動で `pip install` する必要がある
2. **テスト実行が手動**: リグレッションの検知が開発者個人の `pytest` 実行に依存しており、見落としリスクがある
3. **テストマーカーの整理不足**: API を必要とするテスト（`@pytest.mark.api`）と不要なテストの区別が不完全な場合がある
4. **Python バージョン不整合**: `pyproject.toml` は Python 3.11 を指定しているが、実環境は 3.9.6 で動作確認済み。CI で明確にサポート範囲を定義する必要がある

**実装方針**:

| 変更内容 | 詳細 |
|----------|------|
| `pyproject.toml` 更新 | `[project.dependencies]` に `anthropic>=0.20`, `openpyxl>=3.1`, `python-docx>=0.8` 等を明記。`[project.optional-dependencies]` に `dev = ["pytest", "reportlab"]` と `pdf = ["PyMuPDF"]` を追加 |
| `requires-python` 修正 | `>=3.9` に変更（実環境との整合性確保） |
| CI ワークフロー | `.github/workflows/ci.yml` を新規作成。Python 3.9 / 3.11 / 3.12 のマトリクスビルド |
| テスト実行 | `pytest -m "not api"` で API 不要テストのみ CI で実行。API テストは手動 or `workflow_dispatch` で任意実行 |
| キャッシュ | `pip` キャッシュを有効化し、CI の実行時間を短縮 |

**業務への影響**:
- **回答品質の安定化**: コード変更時にリグレッションを自動検知できるため、「前回まで正しく動いていたパイプラインが壊れた」事故を防止できる。FISC質問票の回答は年度単位で運用されるため、年度途中のバグ混入は大きな手戻りになる
- **チーム開発の効率化**: 依存関係が明記されることで、新メンバーは `pip install -e ".[dev]"` の1コマンドで環境構築が完了する。現状は README に手順がなく、暗黙知に依存している
- **変更の安全性向上**: P3（Indexer改善）や P4（Router動的化）といった大きな変更を入れる前に CI を整備しておくことで、リファクタリング中のデグレを即座に検知できる

**FISC制度面の影響**:
- FISC安全対策基準では、システム変更管理プロセスの整備が求められる。CI/CD パイプラインの導入は「変更管理」「テスト管理」の自動化として、**FISC監査時に変更管理体制の成熟度を示す材料** になる
- テスト結果のログが GitHub Actions に自動保存されるため、「いつ・どのコミットで・どのテストが通ったか」のエビデンスが残り、監査対応時のトレーサビリティが向上する

**トークン使用への影響**:
- CI 自体はトークンを消費しない（`-m "not api"` で API テストを除外するため）
- ただし、将来的に API テストを CI で実行する場合は、CI 実行ごとに Reader + Reviewer のトークンが消費される。これを制御するために `workflow_dispatch`（手動トリガー）での API テスト実行を推奨

**影響範囲**:
- `pyproject.toml`（dependencies セクション追加・`requires-python` 変更）
- `.github/workflows/ci.yml`（新規）
- 既存コードへの変更なし

---

### P6: レビューUI（Web UI）

**目的**:
パイプラインが生成した回答結果を、ブラウザ上で対話的に確認・承認・却下できるダッシュボードを提供する。現状は出力Excel を目視確認するワークフローであり、複数人でのレビュー作業や差し戻し管理が煩雑になっている。

**現状の課題**:
1. **レビューフローが Excel ベース**: 出力Excelの「対応状況」列を手動で書き換える運用。複数人が同時にレビューする場合、ファイルの排他制御やマージが問題になる
2. **Reviewer の指摘と対応の紐づけが弱い**: Sheet2「レビュー指摘」に issue_type / severity / description が出力されるが、対応アクション（承認・修正・却下）を記録する仕組みがない
3. **回答履歴の管理がない**: パイプラインを再実行するたびに新しいタイムスタンプ付きExcelが生成されるが、前回からの差分表示や変更理由の記録がない
4. **確信度・フラグの視認性が低い**: Excel上では LOW 確信度や「KB に該当情報なし」フラグが文字列として埋もれており、注意が必要な回答を素早く特定しにくい

**実装方針**:

| コンポーネント | 詳細 |
|---------------|------|
| バックエンド | FastAPI で REST API を構築。パイプライン出力（Excel / JSON）を読み込み、回答・レビュー指摘・承認ステータスを管理 |
| フロントエンド | 軽量 SPA（React or vanilla JS + htmx）。質問一覧 → 回答詳細 → 承認/却下のドリルダウンUI |
| データストア | 初期は SQLite（ファイルベース、インストール不要）。将来的に PostgreSQL 等への移行パスを残す |
| 承認ワークフロー | 回答ごとに「承認」「要修正」「却下」のステータス管理。要修正の場合はコメント付きで差し戻し |
| 最終出力 | 承認済み回答のみを最終 Excel に反映する「確定出力」機能 |
| 差分表示 | 前回回答との差分ハイライト（過去回答再利用 vs 新規回答の区別を視覚化） |

**画面構成（想定）**:
1. **ダッシュボード**: 全質問の一覧表示。確信度・ステータス・レビュー指摘の有無でフィルタリング・ソート
2. **回答詳細**: 質問文・回答文・根拠ソース・確信度・Reviewer指摘を1画面で表示。承認/却下ボタン付き
3. **レビュー指摘一覧**: severity でソートされた指摘リスト。各指摘から該当回答へジャンプ
4. **実行履歴**: パイプライン実行ごとの結果比較。回答の変更箇所をハイライト

**業務への影響**:
- **レビュー工数の大幅削減**: 現状の「Excel を開いて1行ずつ確認 → 別シートの指摘と突き合わせ → 対応状況を手入力」というフローが、「ダッシュボードで LOW 確信度の回答をフィルタ → 指摘を確認 → ワンクリック承認/却下」に短縮される
- **複数人での並行レビュー**: Web UI であれば排他制御の問題がなく、セキュリティ担当・インフラ担当・監査担当がそれぞれの専門領域の回答を並行してレビューできる
- **回答品質の可視化**: 確信度の分布（HIGH/MEDIUM/LOW の割合）、過去回答再利用率、Reviewer 指摘件数などをダッシュボードに表示することで、KB の充実度や回答精度のトレンドを把握できる
- **年度更新作業の効率化**: FISC質問票は毎年更新される。前年度回答との差分表示機能により、「変更のあった質問のみを重点レビュー」というワークフローが可能になる

**FISC制度面の影響**:
- **監査証跡の強化**: 「誰が・いつ・どの回答を承認/却下したか」の記録が自動的に残る。FISC安全対策基準の「変更管理」「承認プロセス」の要件に対するエビデンスとして活用できる
- **回答の一貫性確保**: ダッシュボードで全回答を俯瞰できるため、回答間の矛盾（例: ある回答では「MFA必須」としているのに別の回答では「MFA未導入」としている）を発見しやすくなる。これは現在 Reviewer（Agent 4）が LLM で検出を試みている機能だが、人間によるダブルチェックの場を提供する
- **回答品質の経年管理**: 実行履歴機能により、年度ごとの回答品質推移を追跡できる。「前年度は LOW 確信度が30%だったが、KBを拡充した結果10%に減少した」といった改善の定量化が可能

**トークン使用への影響**:
- **Web UI 自体はトークンを消費しない**: フロントエンド + API サーバーはローカル処理のみ
- **間接的なトークン削減効果**: レビュー結果を次回パイプライン実行にフィードバックする仕組みを入れた場合、「前回承認済みで KB 変更なし」の回答を自動スキップでき、Reader の API 呼び出し回数を削減できる
- **将来的な拡張**: UI 上から「この回答を再生成」（特定質問のみ Reader を再実行）する機能を追加した場合、パイプライン全体ではなく差分実行が可能になり、トークン消費を最小限に抑えられる

**影響範囲**:
- 新規モジュール追加（`src/web/` 配下を想定）。既存のパイプラインコードは変更なし
- 新規依存: `fastapi`, `uvicorn`, `sqlite3`（標準ライブラリ）
- 将来的に `src/models.py` に承認ステータス等のフィールド追加が必要になる可能性あり

**前提**: P3〜P5 が安定してから着手推奨。特に P5（CI/CD）が整備されていることで、UI 追加によるリグレッションを防止できる

---

## トークン消費の全体像

現在のパイプライン（P2時点）のトークン消費構造と、各フェーズでの変化を整理する。

### 現状（P2時点）

| エージェント | API呼び出し回数 | 入力トークン目安 | 出力上限 | 備考 |
|-------------|----------------|-----------------|---------|------|
| Indexer (Agent 1) | 0 | 0 | 0 | ローカル処理のみ |
| Router (Agent 2) | 0 | 0 | 0 | 静的キーワードマッチ |
| Reader (Agent 3) | Reader台数（1〜3） | 各 最大80,000 | 各 4,096 | `token_budget_per_reader` で制御 |
| Reviewer (Agent 4) | 1 | 全回答サマリ（数千〜1万） | 4,096 | 1回のグローバルレビュー |
| **合計** | **2〜4回/パイプライン** | **約90,000〜250,000** | **約12,000〜16,000** | 質問20問・KB12ファイルの場合 |

### 各フェーズでの変化

| フェーズ | 追加トークン | 削減効果 | 純影響 |
|---------|-------------|---------|--------|
| P3（LLM要約OFF） | 0 | 0 | **変化なし** |
| P3（LLM要約ON） | +15,000〜26,000（1回限り・キャッシュ可） | Reader入力の間接削減 | **微増〜相殺** |
| P4（Router動的化） | +4,000〜9,000/回 | 不要ファイル削減 + 過去回答再利用率向上 | **相殺〜純減** |
| P5（CI/CD） | 0（APIテスト除外時） | 0 | **変化なし** |
| P6（Web UI） | 0 | 差分実行による削減（将来） | **変化なし〜削減** |

### コスト試算（参考）

Claude Sonnet 利用時の概算（2025年時点の料金体系）:

- 入力: $3 / 1Mトークン、出力: $15 / 1Mトークン
- 1回のパイプライン実行（20問・KB12ファイル）: 入力約200K + 出力約15K = **約$0.83**
- P4 追加分: 入力約7K + 出力約1K = **約$0.04**（パイプラインあたり約5%増）
- 年間50回実行と仮定: 約$43.5/年（P4込みで約$45.5/年）
